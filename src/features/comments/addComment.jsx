const AddComment = () => {
  return (
    <>
      <form
      // onSubmit={(e) => {
      //     e.preventDefault();
      //     onSubmitComment(comment);
      // }}
      >
        입력 폼
        hi
      </form>
    </>
  );
};

export default AddComment;
