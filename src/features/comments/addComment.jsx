const AddComment = () => {
  return (
    <>
      <form
      // onSubmit={(e) => {
      //     e.preventDefault();
      //     onSubmitComment(comment);
      // }}
      >
        입력 폼
      </form>
    </>
  );
};

export default AddComment;
